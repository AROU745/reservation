# Frontend — Réservation de stock

React + TypeScript + Vite.

## Démarrage

```bash
# Terminal 1 — backend
cd backend
npm run dev

# Terminal 2 — frontend
cd frontend
npm run dev
```

Ouvrir http://localhost:5173

Les appels `/products` sont proxifiés vers `http://localhost:3000`.
